---
phase: 10-chat-y-pdf
plan: 03
type: execute
wave: 2
depends_on:
  - 10-chat-y-pdf-01
  - 10-chat-y-pdf-02
files_modified: []
autonomous: true
requirements:
  - BUILD-01

must_haves:
  truths:
    - "Android APK/AAB build preview is generated via EAS Build"
    - "Build URL is available for sharing and PDF inclusion"
    - "Build completes without errors"
  artifacts:
    - path: "(no file — EAS Build output)"
      provides: "Android APK/AAB binary available on EAS servers"
      contains: "Build artifact (APK or AAB)"
  key_links:
    - from: "EAS Build"
      to: "https://expo.dev/accounts/*/projects/plantasmon/builds/*"
      via: "expo build URL"
      pattern: "expo\\.dev/.*builds"
---

<objective>
Generate an Android build preview (APK/AAB) using EAS Build for review.

Purpose: The user needs an installable Android binary to test the app on a device/emulator and to include screenshots from it in the PDF export. The build also serves as a milestone deliverable.

Output:
- EAS Build artifact (APK/AAB) available at expo.dev
- Build URL captured for use in PDF export
</objective>

<execution_context>
@/home/brian/4_anno/Moviles/Proyecto en clase/plantasmon/.opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/10-chat-y-pdf/10-CONTEXT.md
@eas.json

EAS Build is already configured:
- `preview` profile: `{ "distribution": "internal" }` — generates APK for internal testing
- `production` profile: `{ "autoIncrement": true }` — generates AAB for Play Store

The project's EAS project ID: `112f23e5-4211-4968-968c-b0cbc6bc6a6d`

Authentication: `eas login` may be needed. If EAS CLI is not authenticated, the executor should attempt login using `npx eas login`.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run EAS Android build (preview profile)</name>
  <files>
    (no files modified — CLI command execution)
  </files>

  <read_first>
  - eas.json (verify build profiles)
  - app.json (verify EAS project ID)
  </read_first>

  <action>
  1. Run the EAS Android build with the preview profile:

  ```bash
  cd <project_root>
  npx eas build --platform android --profile preview --non-interactive
  ```

  2. If `--non-interactive` is not supported, use `--no-wait` to submit and return the build URL.

  3. If EAS CLI is not authenticated, attempt:
  ```bash
  npx eas whoami
  ```
  If not logged in, the command will prompt for login. Since this is non-interactive mode, log the requirement: "EAS CLI needs authentication. Run `npx eas login` manually, then retry the build."

  4. Wait for the build to complete. EAS Build typically takes 10-20 minutes.

  5. Once complete, capture the build URL (format: `https://expo.dev/accounts/{account}/projects/plantasmon/builds/{build-id}`).

  6. Log the build URL to console for reference.

  7. If the build fails, read the error logs and troubleshoot. Common issues:
     - Missing Android keystore (EAS handles this automatically)
     - expo-newsarch issues (already enabled in app.json: `"newArchEnabled": true`)
     - Dependency resolution errors
  </action>

  <verify>
  <automated>
  # This command checks build status via EAS API
  npx eas build:list --platform android --limit 1 --non-interactive 2>$null
  </automated>
  </verify>

  <acceptance_criteria>
  - EAS Build completes successfully with status "finished"
  - Build page URL is available and accessible
  - APK/AAB artifact is downloadable from the build page
  - Build URL is logged to console for documentation
  </acceptance_criteria>

  <done>
  Android build preview generated successfully
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify build and document URL</name>
  <files>
    (no files modified — documentation step)
  </files>

  <read_first>
  - (no file reads needed — check the build output from Task 1)
  </read_first>

  <action>
  1. Verify the build completed by checking `npx eas build:list --platform android --limit 1`

  2. Look for the latest build with status "FINISHED" (not "IN_PROGRESS", "ERRORED", or "CANCELED")

  3. Extract the build URL from the build list output

  4. Log the following to the console:
  ```
  ========================================
  ANDROID BUILD COMPLETE
  Build URL: https://expo.dev/accounts/{account}/projects/plantasmon/builds/{build-id}
  Platform: Android
  Profile: preview
  ========================================
  ```

  5. If the build failed, log the error details and suggest fixes:
  - Check EAS dashboard for specific error messages
  - Common fix: run `npx eas build --platform android --profile preview --clear-cache` to retry with clean cache
  </action>

  <verify>
  <automated>
  npx eas build:list --platform android --limit 1 --non-interactive 2>$null | Select-String "finished|FINISHED"
  </automated>
  </verify>

  <acceptance_criteria>
  - Build is confirmed as "FINISHED"
  - Build URL is documented and accessible
  - The APK/AAB can be downloaded from the build page
  </acceptance_criteria>

  <done>
  Android build verified and URL documented
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local machine → EAS servers | Source code and credentials uploaded during build |
| EAS → Google Play (implicit) | Signed AAB could be uploaded if production build |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-09 | Information Disclosure | EAS Build artifacts | accept | Preview builds are internal (distribution: internal); only team members with EAS access can download |
| T-10-10 | Tampering | Build process | accept | EAS builds are fully managed by Expo; no custom build steps that could inject malicious code |
</threat_model>

<verification>

1. `npx eas build:list --platform android --limit 1` shows finished build
2. Build URL opens in browser and shows downloadable artifact
3. No build errors in EAS logs

</verification>

<success_criteria>

1. Android build (APK) is generated successfully via `eas build --platform android --profile preview`
2. Build URL is captured and logged
3. The build is installable on Android device/emulator for testing
4. Build URL can be shared and included in PDF export

</success_criteria>

<output>
After completion, create `.planning/phases/10-chat-y-pdf/10-03-SUMMARY.md`
</output>
