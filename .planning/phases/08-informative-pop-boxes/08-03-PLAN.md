---
phase: 08-informative-pop-boxes
plan: 03
type: execute
wave: 2
depends_on:
  - 08-01-PLAN.md
  - 08-02-PLAN.md
files_modified:
  - src/hooks/useAchievementUnlock.ts
  - app/identify.tsx
  - src/screens/home/HomeScreen.tsx
  - app/journal.tsx
  - app/explore.tsx
  - src/screens/userProfile/UserProfile.tsx
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "Camera/Identificar screen shows first-use explanation popup on first visit"
    - "Home screen shows first-use popup explaining missions section"
    - "Tapping a mission card shows a description tooltip/popover"
    - "Celebration popup appears when claiming a mission reward"
    - "Celebration popup appears when earning an achievement"
    - "Celebration popup appears when obtaining a vitrina item"
    - "Explore screen has info icons on plant cards explaining the UI"
    - "Journal screen shows first-use explanation popup"
    - "Profile screen shows info hints on editable areas"
  artifacts:
    - path: "src/screens/home/HomeScreen.tsx"
      provides: "First-use popup for missions, mission tap info popups, celebration on claim"
      contains: "InfoBottomSheet"
    - path: "app/identify.tsx"
      provides: "First-use explanation popup for camera/identify feature"
      contains: "usePopupDismissal"
    - path: "app/explore.tsx"
      provides: "Info icons on plant cards with sheet explanation"
      contains: "InfoBottomSheet"
    - path: "src/screens/userProfile/UserProfile.tsx"
      provides: "Celebration popup for vitrina item obtain, info hints on editable areas"
      contains: "CelebrationSheet"
    - path: "src/hooks/useAchievementUnlock.ts"
      provides: "Celebration popup trigger on achievement unlock"
      contains: "CelebrationSheet"
  key_links:
    - from: "src/screens/home/HomeScreen.tsx"
      to: "src/hooks/usePopupDismissal.ts"
      via: "import { usePopupDismissal }"
    - from: "app/identify.tsx"
      to: "src/components/ui/InfoBottomSheet.tsx"
      via: "import { InfoBottomSheet }"
    - from: "app/explore.tsx"
      to: "src/components/ui/InfoBottomSheet.tsx"
      via: "import { InfoBottomSheet }"
---

<objective>
**Wire the popup data layer and UI components into all app screens — first-use popups, celebration popups, and info icons per CONTEXT.md screen map.**

Purpose: Per CONTEXT.md "Screens Requiring Popups", six screens need popups integrated:
1. 📷 Camera/Identificar — first-use explanation
2. 🏠 Home/Misiones — first-use for missions, tap info tooltip on mission cards, celebration on mission claim
3. 🖼️ Vitrina/Items — celebration on obtaining a new vitrina item
4. 🔍 Explora/Catálogo — info icon on plant cards
5. 📖 Journal — first-use explanation
6. 👤 Perfil — info hints on editable areas + celebration on achievement unlock

This plan connects the components from Plan 01 and Plan 02 into each screen.

Output:
- Modified `app/identify.tsx` — first-use popup for camera/identify flow
- Modified `src/screens/home/HomeScreen.tsx` — first-use popup + mission tap info + celebration on claim
- Modified `app/journal.tsx` — first-use popup
- Modified `app/explore.tsx` — info icon + bottom sheet on plant cards
- Modified `src/screens/userProfile/UserProfile.tsx` — celebration on vitrina obtain + info hints on ProfileAbout
- Modified `src/hooks/useAchievementUnlock.ts` — optional celebration callback for achievement unlocks
</objective>

<execution_context>
@/home/brian/4_anno/Moviles/Proyecto en clase/plantasmon/.opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/08-informative-pop-boxes/08-CONTEXT.md

# All screen files to modify — read their full content before editing
@app/identify.tsx
@app/journal.tsx
@app/explore.tsx
@src/screens/home/HomeScreen.tsx
@src/screens/userProfile/UserProfile.tsx
@src/hooks/useAchievementUnlock.ts

# Dependencies from Plan 01 & 02 (to be created — executor must create if missing)
# src/services/popupService.ts
# src/hooks/usePopupDismissal.ts
# src/components/ui/InfoBottomSheet.tsx
# src/components/ui/CelebrationSheet.tsx

Excerpts — context for integration:
- HomeScreen already imports: DailyMissions, WeeklyMissions, HomeHeader, LastIdentified, RecentAchievement, StatsBar, TipCard, UserProgress, ScreenWrapper, useThemedStyles, useMissionProgress, userService, missionService, missionData
- identify.tsx imports: ScreenWrapper, useThemedStyles, plantIdService, userPlantsService, expo-router hooks, useNetworkStatus
- UserProfile.tsx imports: Achievements, ActivityFeed, ProfileVitrina, PlantCollection, ProfileAbout, ProfileHero, ScreenWrapper, useProfile, useRouter, obteniblesService, userService
- useAchievementUnlock.ts: hook that listens for achievement unlock events
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate InfoBottomSheet + first-use popup into identify.tsx (Camera/Identificar)</name>
  <files>app/identify.tsx</files>
  <read_first>
    @app/identify.tsx
  </read_first>
  <action>
    Per CONTEXT.md "Screens Requiring Popups" — Camera/Identificar needs a first-use explanation popup.

    1. Add imports at top of file:
    ```tsx
    import { InfoBottomSheet } from "@/src/components/ui/InfoBottomSheet"
    import { usePopupDismissal } from "@/src/hooks/usePopupDismissal"
    ```

    2. Inside the component function (after the `const [error, setError]` line), add popup state:
    ```tsx
    const identifyPopup = usePopupDismissal({ popupKey: "identify_first_use" })
    ```

    3. Add the first-use popup JSX inside the return, before the closing ScreenWrapper tag (at the end, wrapped in a conditional or using the `visible` prop):
    ```tsx
    <InfoBottomSheet
      visible={identifyPopup.visible}
      title="📸 Identificar una planta"
      message="Sacá una foto de una planta para identificarla con nuestra base de datos. La app analiza la foto y te muestra el nombre, cuidados y más información. Asegurate de tener buena iluminación."
      icon="🌱"
      showDontShowAgain={true}
      onDismiss={identifyPopup.dismiss}
      onDontShowAgain={identifyPopup.dismissForeverFn}
    />
    ```

    **Only add the popup JSX if the screen doesn't already have a bottom sheet or modal for first-use guidance.** Use discretion to place it at the end of the return (after the closing `</View>` or `<ScrollView>` but before `</ScreenWrapper>`).

    **IMPORTANT:** Do not refactor or restyle any existing identify screen code. Only add the imports, state initialization, and popup JSX.
  </action>
  <acceptance_criteria>
    - `app/identify.tsx` imports `InfoBottomSheet` and `usePopupDismissal`
    - File contains `usePopupDismissal({ popupKey: "identify_first_use" })`
    - File contains `<InfoBottomSheet>` JSX with `icon="🌱"` and title starting with "📸"
    - All existing functionality preserved
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <done>identify.tsx shows first-use popup on first visit, compiles clean</done>
</task>

<task type="auto">
  <name>Task 2: Integrate InfoBottomSheet + CelebrationSheet into HomeScreen</name>
  <files>src/screens/home/HomeScreen.tsx</files>
  <read_first>
    @src/screens/home/HomeScreen.tsx
  </read_first>
  <action>
    Per CONTEXT.md — Home/Misiones needs:
    - First-use explanation popup for the missions section
    - Per D-03: Info icon (ℹ️) on mission cards showing description
    - Celebration popup when claiming mission rewards

    1. Add imports:
    ```tsx
    import { InfoBottomSheet } from "@/src/components/ui/InfoBottomSheet"
    import { CelebrationSheet } from "@/src/components/ui/CelebrationSheet"
    import { usePopupDismissal } from "@/src/hooks/usePopupDismissal"
    ```

    2. Add popup state hooks (near existing `useMissionProgress()` line):
    ```tsx
    const missionsPopup = usePopupDismissal({ popupKey: "missions_first_use" })
    const [claimedCelebration, setClaimedCelebration] = useState<{ title: string; message: string; icon: string } | null>(null)
    ```

    3. Modify the `onClaim` callback for missions. Find where `claimMissionReward` is called. After the reward is claimed, set the celebration state:
    ```tsx
    // After successful claim, show celebration
    const handleClaim = async (missionId: string) => {
      // find existing claim logic and wrap/append
      // After claim success:
      setClaimedCelebration({
        title: "¡Misión completada! 🎉",
        message: `Has ganado XP por completar la misión. ¡Seguí así!`,
        icon: "⭐",
      })
    }
    ```
    If the existing `onClaim` already handles the logic, wrap the claim call inside a new `handleClaim` function and pass it to `DailyMissions.onClaim`.

    4. Add popup JSX at bottom of return (before closing `</ScreenWrapper>` or at end of component return):
    ```tsx
    <InfoBottomSheet
      visible={missionsPopup.visible}
      title="🏠 Misiones diarias"
      message="Completá misiones para ganar experiencia y desbloquear logros. Tocá una misión para ver más detalles. Las misiones se renuevan todos los días."
      icon="📋"
      showDontShowAgain={true}
      onDismiss={missionsPopup.dismiss}
      onDontShowAgain={missionsPopup.dismissForeverFn}
    />
    
    {claimedCelebration && (
      <CelebrationSheet
        visible={!!claimedCelebration}
        title={claimedCelebration.title}
        message={claimedCelebration.message}
        icon={claimedCelebration.icon}
        onDismiss={() => setClaimedCelebration(null)}
      />
    )}
    ```

    5. **Mission card info tooltip:** Per D-03 and CONTEXT.md "Misiones: tap on mission card shows tooltip/popover with description". The existing `DailyMissions` component renders mission cards. Rather than modifying `DailyMissions.tsx` (which would require changing its interface), add a `useState<string | null>` for `missionInfoKey` in HomeScreen and render an `InfoBottomSheet` when a mission is tapped. Wrap each mission's `onPress` in the render or use the existing claim interaction.

    Since `DailyMissions` doesn't expose an "onTap" separately from "onClaim", implement the info popup by wrapping the missions area with a simple explanation popup that shows when the user first taps any mission card. Alternatively, add a small "ℹ️" info icon next to the "Misiones" section header that opens an InfoBottomSheet. Add this:
    ```tsx
    const [showMissionInfo, setShowMissionInfo] = useState(false)
    ```
    And add an info icon next to the section header (before `<DailyMissions>`) with TouchableOpacity that calls `setShowMissionInfo(true)`.

    **IMPORTANT:** Preserve ALL existing imports, logic, styles, and component behavior. Only add new imports and popup wiring.
  </action>
  <acceptance_criteria>
    - HomeScreen imports `InfoBottomSheet`, `CelebrationSheet`, `usePopupDismissal`
    - Contains `usePopupDismissal({ popupKey: "missions_first_use" })`
    - Contains `CelebrationSheet` for mission claim celebration
    - Contains info icon trigger for mission explanation
    - All existing functionality preserved (missions, stats, achievements, etc.)
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <done>HomeScreen has first-use popup, mission info, and claim celebration, compiles clean</done>
</task>

<task type="auto">
  <name>Task 3: Integrate popups into Journal, Explore, and Profile screens</name>
  <files>app/journal.tsx, app/explore.tsx, src/screens/userProfile/UserProfile.tsx, src/hooks/useAchievementUnlock.ts</files>
  <read_first>
    @app/journal.tsx
    @app/explore.tsx
    @src/screens/userProfile/UserProfile.tsx
    @src/hooks/useAchievementUnlock.ts
  </read_first>
  <action>
    **3a: Journal screen (`app/journal.tsx`)**
    Per CONTEXT.md — Journal needs first-use explanation popup showing what each section means.

    1. Add imports:
    ```tsx
    import { InfoBottomSheet } from "@/src/components/ui/InfoBottomSheet"
    import { usePopupDismissal } from "@/src/hooks/usePopupDismissal"
    ```
    2. Add state hook:
    ```tsx
    const journalPopup = usePopupDismissal({ popupKey: "journal_first_use" })
    ```
    3. Add popup JSX before the closing outermost tag:
    ```tsx
    <InfoBottomSheet
      visible={journalPopup.visible}
      title="📖 Tu diario de plantas"
      message="Acá vas a encontrar un resumen de tu actividad: las plantas que identificaste, tus misiones activas, tu progreso y los últimos logros obtenidos."
      icon="🌿"
      showDontShowAgain={true}
      onDismiss={journalPopup.dismiss}
      onDontShowAgain={journalPopup.dismissForeverFn}
    />
    ```

    **3b: Explore screen (`app/explore.tsx`)**
    Per CONTEXT.md — Explora/Catálogo needs info icon on plant cards explaining the UI (not plant biology).

    1. Add imports:
    ```tsx
    import { InfoBottomSheet } from "@/src/components/ui/InfoBottomSheet"
    import { useState } from "react"
    ```
    2. Add state for info popup:
    ```tsx
    const [showCatalogInfo, setShowCatalogInfo] = useState(false)
    ```
    3. **Locate the section header or title area** in the explore screen rendering. Add an info icon (ℹ️) as a `TouchableOpacity` next to the title/header:
    ```tsx
    <TouchableOpacity onPress={() => setShowCatalogInfo(true)}>
      <Text style={{ fontSize: 18, color: COLORS.foreground }}>ℹ️</Text>
    </TouchableOpacity>
    ```
    Import COLORS: `import { COLORS } from "@/src/constants/theme"` if not already imported.

    4. Add popup JSX:
    ```tsx
    <InfoBottomSheet
      visible={showCatalogInfo}
      title="🔍 Explorar plantas"
      message="Navegá el catálogo completo de plantas. Usá la lupa para buscar por nombre. Tocá una planta para ver sus detalles y cuidados."
      icon="🌵"
      showDontShowAgain={true}
      onDismiss={() => setShowCatalogInfo(false)}
      onDontShowAgain={() => { setShowCatalogInfo(false); /* optional permanent dismiss handled by caller */ }}
    />
    ```

    **3c: Profile screen (`src/screens/userProfile/UserProfile.tsx`)**
    Per CONTEXT.md — Perfil needs:
    - Celebration popup when obtaining a new vitrina item. The `getObteniblesWithStatus` already runs in the existing `useEffect`. After it loads, check for newly obtained items and show celebration.
    - Per D-03: Info hints on editable areas (ProfileAbout section — "Esto se edita presionando el botón Editar perfil")

    1. Add imports:
    ```tsx
    import { CelebrationSheet } from "@/src/components/ui/CelebrationSheet"
    import { InfoBottomSheet } from "@/src/components/ui/InfoBottomSheet"
    import { useState } from "react"  // if not already imported
    ```
    2. Add celebration state:
    ```tsx
    const [vitrinaCelebration, setVitrinaCelebration] = useState<{ title: string; message: string; icon: string } | null>(null)
    const [showEditInfo, setShowEditInfo] = useState(false)
    ```
    3. After the vitrina items load in the existing useEffect, detect if there's a newly obtained item. Add after the `getObteniblesWithStatus` call:
    ```tsx
    // Check for recently obtained items (within last minute)
    const recentItems = items.filter(i => i.obtained && i.obtainedAt && (Date.now() - new Date(i.obtainedAt).getTime()) < 60000)
    if (recentItems.length > 0) {
      const item = recentItems[0]
      setVitrinaCelebration({
        title: "🎉 ¡Nuevo objeto!",
        message: `Has obtenido "${item.name}" en tu vitrina.`,
        icon: item.icon || "🏆",
      })
    }
    ```
    Note: Check if `ObtenibleDisplay` has an `obtainedAt` field. If not, skip the time check and show celebration for any obtained items that weren't previously tracked. Use best judgment based on actual `ObtenibleDisplay` type.

    4. Add celebration popup JSX before the closing tag:
    ```tsx
    {vitrinaCelebration && (
      <CelebrationSheet
        visible={!!vitrinaCelebration}
        title={vitrinaCelebration.title}
        message={vitrinaCelebration.message}
        icon={vitrinaCelebration.icon}
        onDismiss={() => setVitrinaCelebration(null)}
      />
    )}
    ```

    5. **Info hint on editable areas:** In the `ProfileAbout` section, locate where bio/location are displayed. Add an info icon next to the section:
    ```tsx
    <TouchableOpacity onPress={() => setShowEditInfo(true)}>
      <Text style={{ fontSize: 14, marginLeft: 8 }}>ℹ️</Text>
    </TouchableOpacity>
    
    <InfoBottomSheet
      visible={showEditInfo}
      title="👤 Editar perfil"
      message="Esto se edita presionando el botón Editar perfil en la parte superior de la pantalla."
      icon="✏️"
      onDismiss={() => setShowEditInfo(false)}
    />
    ```

    **3d: Achievement unlock celebration (`src/hooks/useAchievementUnlock.ts`)**
    Read this file first — if it's a hook that detects achievement unlocks, add a callback mechanism for celebration popups.

    1. Add a new optional callback parameter or return value:
    ```tsx
    // At the top, add import
    import { useState } from "react"
    
    // In the return, add:
    // latestUnlock: { name: string; description: string; icon: string } | null
    ```
    2. Expose `latestUnlock` state that tracks the most recent achievement unlock. Return it alongside existing values so the Profile screen or any consumer can render a CelebrationSheet.

    **If `useAchievementUnlock.ts` doesn't exist or has a different interface**, adapt the approach: create a lightweight mechanism that allows screens to detect new unlocks. The simplest approach is to check `userAchievements` against a previously stored count.

    **IMPORTANT for ALL screen integrations:** Preserve ALL existing imports, logic, styles, and component behavior. Only add new imports and popup JSX. Do NOT reformat or restyle existing code.
  </action>
  <acceptance_criteria>
    - `app/journal.tsx` has first-use popup with `popupKey: "journal_first_use"`
    - `app/explore.tsx` has info icon trigger and `InfoBottomSheet` for catalog explanation
    - `src/screens/userProfile/UserProfile.tsx` has `CelebrationSheet` for vitrina items
    - `src/screens/userProfile/UserProfile.tsx` has info hint popup for editable areas
    - `src/hooks/useAchievementUnlock.ts` exposes celebration data for latest unlock
    - All files pass `npx tsc --noEmit`
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <done>All screens have appropriate popups, TypeScript compiles clean</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client to Firestore | Celebration state reads user achievement/obtenible data from Firestore |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-04 | Info Disclosure | Achievement/Obtenible data in popups | accept | Data shown is the same data already visible on Profile/Home screens — no additional exposure |
| T-08-05 | Tampering | usePopupDismissal popupKey | accept | Keys are hardcoded constants per component, not user-controllable strings |
</threat_model>

<verification>
1. `npx tsc --noEmit` passes across all modified files
2. Each screen listed in CONTEXT.md "Screens Requiring Popups" has appropriate popup integration
3. Celebration popups show on: mission claim, achievement unlock, vitrina item obtain
4. First-use popups show on: Identify, Home missions, Journal
5. Info icons/manual triggers on: Explore catalog, Profile editable areas, Home missions header
6. No existing functionality is broken or refactored
</verification>

<success_criteria>
- All 6 screens from CONTEXT.md have popup integration
- First-use popups with "No mostrar de nuevo" wired to AsyncStorage dismissal
- Celebration popups on all unlock types (achievement, mission, vitrina item)
- Info icons on cards/areas that need explanation
- TypeScript compiles without errors
- All existing screen functionality preserved
</success_criteria>
