---
phase: 05-binnacle-test-and-qa-clase-asincronica-pdf
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - src/screens/auth/loginScreen.tsx
  - src/screens/auth/__tests__/loginScreen.test.tsx
autonomous: true
requirements:
  - QA-02
user_setup: []
must_haves:
  truths:
    - "LoginScreen muestra error en español cuando email/contraseña están vacíos."
    - "La prueba de componente valida el mensaje exacto de error."
  artifacts:
    - path: "src/screens/auth/__tests__/loginScreen.test.tsx"
      provides: "Test de componente para LoginScreen"
  key_links:
    - from: "src/screens/auth/__tests__/loginScreen.test.tsx"
      to: "src/screens/auth/loginScreen.tsx"
      via: "render del componente"
      pattern: "LoginScreen"
---

<objective>
Crear una prueba de componente para LoginScreen que verifique el mensaje de error en español cuando el formulario se envía vacío.

Purpose: cubrir flujo crítico de validación de login en UI.
Output: test de componente ejecutable con RTL.
</objective>

<execution_context>
@/home/brian/4_anno/Moviles/Proyecto en clase/plantasmon/.opencode/get-shit-done/workflows/execute-plan.md
@/home/brian/4_anno/Moviles/Proyecto en clase/plantasmon/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STRUCTURE.md
@.planning/phases/05-binnacle-test-and-qa-clase-asincronica-pdf/05-CONTEXT.md
@src/screens/auth/loginScreen.tsx
@src/components/ui/button.tsx

<interfaces>
From src/screens/auth/loginScreen.tsx:
```ts
const handleLogin = async () => {
  if (!email.trim() || !password.trim()) {
    setError("Por favor completa todos los campos.")
    return
  }
}
```

From src/components/ui/button.tsx:
```ts
function Button({ children, ...props }: ButtonProps) {
  return (
    <Pressable {...props}>
      <Text>{children}</Text>
    </Pressable>
  )
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Añadir test de componente para error de LoginScreen</name>
  <files>src/screens/auth/__tests__/loginScreen.test.tsx, src/screens/auth/loginScreen.tsx</files>
  <read_first>
    src/screens/auth/loginScreen.tsx
    src/components/ui/button.tsx
    .planning/codebase/CONVENTIONS.md
    .planning/codebase/STRUCTURE.md
  </read_first>
  <action>
    Crea src/screens/auth/__tests__/loginScreen.test.tsx con contenido exacto. Sigue el patrón de 05-PATTERNS.md:
    - Mensaje exacto del error desde `src/screens/auth/loginScreen.tsx` ("Por favor completa todos los campos.").
    - El bloque UI de error es `{error && (<View ...><Text ...>{error}</Text></View>)}` y debe ser renderizado por el test.
    - No hay analog exacto para test de componente: usar guía de `.planning/codebase/TESTING.md` (RTL) y mocks mínimos para dependencias nativas.
    ```ts
    import React from "react"
    import { render, fireEvent } from "@testing-library/react-native"
    import LoginScreen from "@/src/screens/auth/loginScreen"

    jest.mock("expo-router", () => ({
      useRouter: () => ({ push: jest.fn() }),
    }))

    jest.mock("react-native-safe-area-context", () => ({
      useSafeAreaInsets: () => ({ top: 0, bottom: 0, left: 0, right: 0 }),
      SafeAreaView: ({ children }: any) => children,
    }))

    jest.mock("expo-auth-session/providers/google", () => ({
      useIdTokenAuthRequest: () => [null, null, jest.fn()],
    }))

    jest.mock("firebase/auth", () => ({
      GoogleAuthProvider: { credential: jest.fn() },
      signInWithCredential: jest.fn(),
    }))

    jest.mock("@/src/config/firebase", () => ({
      auth: {},
    }))

    describe("LoginScreen", () => {
      it("muestra error cuando el formulario está vacío", () => {
        const { getByText, queryByText } = render(<LoginScreen />)
        expect(queryByText("Por favor completa todos los campos.")).toBeNull()

        fireEvent.press(getByText("Entrar"))

        expect(getByText("Por favor completa todos los campos.")).toBeTruthy()
      })
    })
    ```

    Si el botón "Entrar" no es encontrable por texto, agrega en loginScreen.tsx
    un prop testID="login-submit" al componente <Button> de Entrar y usa en el test:
    fireEvent.press(getByTestId("login-submit"))
  </action>
  <acceptance_criteria>
    - Existe src/screens/auth/__tests__/loginScreen.test.tsx.
    - El test usa el texto exacto "Por favor completa todos los campos.".
    - El test presiona el botón de login y afirma que el error aparece.
    - Si se agregó testID, loginScreen.tsx contiene testID="login-submit".
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --runTestsByPath src/screens/auth/__tests__/loginScreen.test.tsx</automated>
  </verify>
  <done>
    El test de componente pasa y valida el mensaje de error en LoginScreen.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test runner → UI component | RTL renderiza LoginScreen con mocks |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | T | loginScreen.test.tsx | mitigate | Mockear expo-router, auth y safe-area para evitar fallos por dependencias nativas. |
| T-05-05 | I | LoginScreen | accept | Test no expone datos sensibles; solo valida mensaje de error. |
</threat_model>

<verification>
  - `npm test -- --runTestsByPath src/screens/auth/__tests__/loginScreen.test.tsx` pasa
</verification>

<success_criteria>
- Test de componente valida error de formulario vacío en LoginScreen
</success_criteria>

<output>
After completion, create `.planning/phases/05-binnacle-test-and-qa-clase-asincronica-pdf/05-02-SUMMARY.md`
</output>
