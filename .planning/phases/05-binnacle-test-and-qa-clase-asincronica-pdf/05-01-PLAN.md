---
phase: 05-binnacle-test-and-qa-clase-asincronica-pdf
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - babel.config.js
  - jest.config.js
  - jest.setup.ts
  - src/lib/plantValidation.ts
  - src/lib/__tests__/plantValidation.test.ts
autonomous: true
requirements:
  - QA-02
user_setup: []
must_haves:
  truths:
    - "Se puede ejecutar `npm test` y Jest corre en el proyecto Expo."
    - "La validación de datos de planta rechaza entradas inválidas con mensajes en español."
  artifacts:
    - path: "jest.config.js"
      provides: "Configuración Jest para Expo con alias @/ y setupFilesAfterEnv"
    - path: "jest.setup.ts"
      provides: "Extensiones de jest-native activas"
    - path: "src/lib/plantValidation.ts"
      provides: "Helper validatePlantData para pruebas unitarias"
    - path: "src/lib/__tests__/plantValidation.test.ts"
      provides: "Pruebas unitarias de validación"
  key_links:
    - from: "src/lib/__tests__/plantValidation.test.ts"
      to: "src/lib/plantValidation.ts"
      via: "import validatePlantData"
      pattern: "validatePlantData"
    - from: "jest.config.js"
      to: "jest.setup.ts"
      via: "setupFilesAfterEnv"
      pattern: "jest\.setup\.ts"
---

<objective>
Configurar Jest + React Native Testing Library en Expo y crear una prueba unitaria real para validación de datos de planta.

Purpose: habilitar QA básica con tests reproducibles.
Output: configuración de Jest + helper de validación + test unitario ejecutable.
</objective>

<execution_context>
@/home/brian/4_anno/Moviles/Proyecto en clase/plantasmon/.opencode/get-shit-done/workflows/execute-plan.md
@/home/brian/4_anno/Moviles/Proyecto en clase/plantasmon/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/TESTING.md
@.planning/codebase/STACK.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STRUCTURE.md
@.planning/phases/05-binnacle-test-and-qa-clase-asincronica-pdf/05-CONTEXT.md
@src/services/userPlantsService.ts

<interfaces>
From src/services/userPlantsService.ts:
```ts
export interface AddPlantFromIdInput {
  plantId: string;
  imageUri: string;
  userId?: string;
  commonName?: string;
  scientificName?: string;
  waterSchedule?: string;
  sunlight?: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configurar Jest + RTL en Expo</name>
  <files>package.json, babel.config.js, jest.config.js, jest.setup.ts</files>
  <read_first>
    package.json
    .planning/codebase/TESTING.md
    .planning/codebase/STACK.md
    .planning/codebase/CONVENTIONS.md
    .planning/codebase/STRUCTURE.md
  </read_first>
  <action>
    Actualiza package.json con script y devDependencies exactas:
    - scripts.test = "jest --watchAll=false"
    - devDependencies agrega:
      "jest": "^29.7.0"
      "jest-expo": "~54.0.0"
      "babel-jest": "^29.7.0"
      "@testing-library/react-native": "^12.5.1"
      "@testing-library/jest-native": "^5.4.2"
      "react-test-renderer": "19.1.0"
      "@types/jest": "^29.5.12"

    Crea babel.config.js con contenido exacto:
    ```js
    module.exports = function (api) {
      api.cache(true)
      return {
        presets: ["babel-preset-expo"],
      }
    }
    ```

    Crea jest.config.js con contenido exacto:
    ```js
    module.exports = {
      preset: "jest-expo",
      setupFilesAfterEnv: ["<rootDir>/jest.setup.ts"],
      moduleNameMapper: {
        "^@/(.*)$": "<rootDir>/$1",
      },
      testMatch: ["**/__tests__/**/*.test.(ts|tsx)"],
      moduleFileExtensions: ["ts", "tsx", "js", "jsx", "json"],
      transformIgnorePatterns: [
        "node_modules/(?!((jest-)?react-native|@react-native|@react-native-community|expo(nent)?|expo-router|@expo(nent)?|@expo-google-fonts|react-navigation|@react-navigation|@sentry/react-native)/)"
      ]
    }
    ```

    Crea jest.setup.ts con contenido exacto:
    ```ts
    import "@testing-library/jest-native/extend-expect"
    ```
  </action>
  <acceptance_criteria>
    - package.json contiene "test": "jest --watchAll=false" en scripts.
    - package.json contiene devDependency "jest-expo": "~54.0.0".
    - jest.config.js contiene la clave preset con valor "jest-expo".
    - jest.config.js contiene moduleNameMapper con "^@/(.*)$": "<rootDir>/$1".
    - jest.setup.ts contiene la línea import "@testing-library/jest-native/extend-expect".
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --help</automated>
  </verify>
  <done>
    Jest puede invocarse y la configuración existe en el repo.
  </done>
</task>

<task type="auto">
  <name>Task 2: Helper de validación de planta + test unitario</name>
  <files>src/lib/plantValidation.ts, src/lib/__tests__/plantValidation.test.ts</files>
  <read_first>
    src/services/userPlantsService.ts
    src/schemas/editProfileSchema.ts
    .planning/codebase/CONVENTIONS.md
    .planning/codebase/STRUCTURE.md
    .planning/phases/05-binnacle-test-and-qa-clase-asincronica-pdf/05-CONTEXT.md
  </read_first>
  <action>
    Crea src/lib/plantValidation.ts con el helper exportado (en español) y tipos exactos. Sigue los patrones de 05-PATTERNS.md:
    - Usa estilo de helper puro como `src/services/userPlantsService.ts` (función pequeña, sin efectos colaterales).
    - Mensajes en español con estilo Zod como `src/schemas/editProfileSchema.ts` (ej: "El nombre debe tener al menos 2 caracteres").
    - No hay analog exacto para test unitario: usa guía de `.planning/codebase/TESTING.md` (Jest/RTL) y mantén pruebas co-localizadas en `__tests__`.
    ```ts
    export type PlantValidationInput = {
      commonName: string
      scientificName: string
      wateringDays: number
      sunlight: "low" | "partial" | "full"
    }

    export type PlantValidationResult = {
      valid: boolean
      errors: string[]
    }

    export function validatePlantData(input: PlantValidationInput): PlantValidationResult {
      const errors: string[] = []

      if (!input.commonName.trim() || input.commonName.trim().length < 2) {
        errors.push("El nombre común debe tener al menos 2 caracteres")
      }
      if (!input.scientificName.trim() || input.scientificName.trim().length < 3) {
        errors.push("El nombre científico debe tener al menos 3 caracteres")
      }
      if (!Number.isFinite(input.wateringDays) || input.wateringDays < 1 || input.wateringDays > 30) {
        errors.push("El riego debe estar entre 1 y 30 días")
      }
      if (!["low", "partial", "full"].includes(input.sunlight)) {
        errors.push("La luz debe ser low, partial o full")
      }

      return { valid: errors.length === 0, errors }
    }
    ```

    Crea src/lib/__tests__/plantValidation.test.ts con pruebas exactas:
    ```ts
    import { validatePlantData } from "@/src/lib/plantValidation"

    describe("validatePlantData", () => {
      it("acepta datos válidos", () => {
        const result = validatePlantData({
          commonName: "Monstera",
          scientificName: "Monstera deliciosa",
          wateringDays: 7,
          sunlight: "partial",
        })
        expect(result.valid).toBe(true)
        expect(result.errors).toEqual([])
      })

      it("reporta errores cuando hay datos inválidos", () => {
        const result = validatePlantData({
          commonName: "",
          scientificName: "A",
          wateringDays: 0,
          sunlight: "shade" as any,
        })
        expect(result.valid).toBe(false)
        expect(result.errors).toEqual(
          expect.arrayContaining([
            "El nombre común debe tener al menos 2 caracteres",
            "El nombre científico debe tener al menos 3 caracteres",
            "El riego debe estar entre 1 y 30 días",
            "La luz debe ser low, partial o full",
          ])
        )
      })
    })
    ```
  </action>
  <acceptance_criteria>
    - src/lib/plantValidation.ts exporta validatePlantData y PlantValidationInput.
    - src/lib/plantValidation.ts contiene el mensaje "El riego debe estar entre 1 y 30 días".
    - src/lib/__tests__/plantValidation.test.ts contiene describe("validatePlantData").
    - El test inválido verifica los 4 mensajes de error exactos en español (estilo Zod, con "El/La").
  </acceptance_criteria>
  <verify>
    <automated>npm test -- --runTestsByPath src/lib/__tests__/plantValidation.test.ts</automated>
  </verify>
  <done>
    El test unitario pasa y valida reglas mínimas de datos de planta.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test runner → app code | Jest ejecuta lógica de validación y mocks de RN |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | T | jest.config.js | mitigate | Configurar preset jest-expo y moduleNameMapper para evitar imports rotos que alteren resultados de prueba. |
| T-05-02 | I | plantValidation.ts | accept | No hay PII; errores son mensajes locales de validación. |
| T-05-03 | D | test runner | accept | Tests son locales y de bajo impacto; no se ejecutan en producción. |
</threat_model>

<verification>
- `npm test -- --runTestsByPath src/lib/__tests__/plantValidation.test.ts` pasa
</verification>

<success_criteria>
- Jest instalado y ejecutable desde script npm
- Helper de validación con mensajes en español probado por test unitario
</success_criteria>

<output>
After completion, create `.planning/phases/05-binnacle-test-and-qa-clase-asincronica-pdf/05-01-SUMMARY.md`
</output>
