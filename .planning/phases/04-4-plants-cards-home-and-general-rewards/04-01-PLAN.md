---
phase: 04-4-plants-cards-home-and-general-rewards
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/services/userService.ts
  - src/services/userPlantsService.ts
  - src/components/home/StatsBar.tsx
  - src/screens/home/HomeScreen.tsx
  - src/styles/themedStyles.ts
  - src/components/home/PlantOfTheDay.tsx
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "User can see account age (days since createdAt) on Home stats bar"
    - "User can see photos taken today on Home stats bar"
    - "User can see watering streak on Home stats bar"
    - "User can see last identified plant (name + date) on Home stats bar"
    - "PlantOfTheDay no longer shows on Home (stubbed/hidden per D-02)"
  artifacts:
    - path: "src/components/home/StatsBar.tsx"
      provides: "Stats bar component with 4 stat items"
      min_lines: 80
    - path: "src/screens/home/HomeScreen.tsx"
      provides: "HomeScreen with StatsBar integrated, PlantOfTheDay removed"
      contains: "StatsBar"
    - path: "src/styles/themedStyles.ts"
      provides: "createStatsBarStyles in stylesByComponent"
      contains: "statsBar"
    - path: "src/components/home/PlantOfTheDay.tsx"
      provides: "Stubbed PlantOfTheDay returning null"
      contains: "return null"
    - path: "src/services/userService.ts"
      provides: "getAccountAge helper and logWateringActivity"
      exports: ["getAccountAge", "logWateringActivity"]
    - path: "src/services/userPlantsService.ts"
      provides: "getPhotosTodayCount and getLastIdentification"
      exports: ["getPhotosTodayCount", "getLastIdentification"]
  key_links:
    - from: "HomeScreen.tsx" to: "StatsBar" via: "component import" pattern: "StatsBar"
    - from: "StatsBar.tsx" to: "userService" via: "import" pattern: "userService"
    - from: "StatsBar.tsx" to: "userPlantsService" via: "import" pattern: "userPlantsService"
    - from: "StatsBar.tsx" to: "themedStyles" via: "useThemedStyles" pattern: "statsBar"

---

<objective>
Add a horizontal stats bar below HomeHeader with 4 real-data stats (account age, photos today, watering streak, last identification) and hide PlantOfTheDay per D-02.

Purpose: Replace the current HomeHeader hardcoded badges with a proper stats bar wired to Firestore. Home becomes useful at a glance.
Output: StatsBar component, service helpers, updated HomeScreen, stubbed PlantOfTheDay, new themed style set.
</objective>

<execution_context>
@/home/brian/4_anno/Moviles/Proyecto en clase/plantasmon/.opencode/get-shit-done/workflows/execute-plan.md
@/home/brian/4_anno/Moviles/Proyecto en clase/plantasmon/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/04-4-plants-cards-home-and-general-rewards/04-CONTEXT.md

@src/screens/home/HomeScreen.tsx
@src/components/home/HomeHeader.tsx
@src/components/home/PlantOfTheDay.tsx
@src/services/userService.ts
@src/services/userPlantsService.ts
@src/styles/themedStyles.ts
@src/constants/data.ts
</context>

<tasks>

<task type="auto">
<name>Task 1: Add stats helpers to userService and userPlantsService</name>
<files>
  src/services/userService.ts
  src/services/userPlantsService.ts
</files>
<read_first>
  @src/services/userService.ts
  @src/services/userPlantsService.ts
</read_first>
<action>
Add getAccountAge and logWateringActivity to userService.ts.
Add getPhotosTodayCount and getLastIdentification to userPlantsService.ts.

getAccountAge(joinDate: Date): number — calculates days since account creation.
logWateringActivity(userId): async — reads user doc, checks lastWateredDate, updates stats.streakDays using this logic:
- If lastWateredDate is today: no change (already logged)
- If lastWateredDate is yesterday: increment streak by 1
- If lastWateredDate is older: reset streak to 1
- If no lastWateredDate: set streak to 1

getPhotosTodayCount(userId): async — reads userPlants, filters those where firstIdentifiedAt date matches today's date, returns count.
getLastIdentification(userId): async — sorts userPlants by firstIdentifiedAt desc, takes most recent, fetches plant doc for commonName, returns { plantName, date }.
</action>
<acceptance_criteria>
- userService.ts exports getAccountAge function
- userService.ts exports logWateringActivity async function
- userPlantsService.ts exports getPhotosTodayCount async function
- userPlantsService.ts exports getLastIdentification async function
- npx tsc --noEmit exits 0
</acceptance_criteria>
<verify>
  <automated>grep -n "export function getAccountAge\|export async function logWateringActivity" src/services/userService.ts && grep -n "export async function getPhotosTodayCount\|export async function getLastIdentification" src/services/userPlantsService.ts && npx tsc --noEmit 2>&1 | head -5</automated>
</verify>
<done>Both services have the 4 new helper functions exported. TypeScript compiles without errors.</done>
</task>

<task type="auto">
<name>Task 2: Create StatsBar component and themed styles</name>
<files>
  src/components/home/StatsBar.tsx
  src/styles/themedStyles.ts
</files>
<read_first>
  @src/components/home/HomeHeader.tsx
  @src/styles/themedStyles.ts
  @src/services/userService.ts
  @src/services/userPlantsService.ts
</read_first>
<action>
1. Create src/components/home/StatsBar.tsx:
   - Uses useThemedStyles("statsBar") for all styles
   - Uses useFocusEffect (from expo-router) to fetch stats on mount
   - Fetches 4 data points: account age, photos today, watering streak, last identification
   - Displays as a horizontal row of 4 stat items with emoji + value + label
   - Icons/labels: "📅 Días activo", "📸 Fotos hoy", "🔥 Racha de riego", "🌱 Última identificación"
   - Shows loading skeleton (4 dim rectangles) while data fetches
   - Catches errors silently (individual try/catch per stat) — shows "—" for failed stats
   - Last date shown as relative: "Hoy", "Ayer", "Hace N días"

2. Add to src/styles/themedStyles.ts:
   - createStatsBarStyles: container (row), statItem (flex:1), statIcon, statValue (fontWeight:800), statLabel (fontSize:9, textTertiary), skeleton
   - Add statsBar to stylesByComponent map + StylesForComponent conditional type
</action>
<acceptance_criteria>
- src/components/home/StatsBar.tsx exists with exported StatsBar function
- src/styles/themedStyles.ts has createStatsBarStyles function
- src/styles/themedStyles.ts has statsBar key in stylesByComponent map
- src/styles/themedStyles.ts has statsBar in StylesForComponent conditional type
- npx tsc --noEmit exits 0
</acceptance_criteria>
<verify>
  <automated>test -f src/components/home/StatsBar.tsx && grep -q "createStatsBarStyles" src/styles/themedStyles.ts && grep -q "statsBar" src/styles/themedStyles.ts && npx tsc --noEmit 2>&1 | head -5</automated>
</verify>
<done>StatsBar component exists, styles are registered. TypeScript compiles.</done>
</task>

<task type="auto">
<name>Task 3: Wire StatsBar into HomeScreen and stub PlantOfTheDay</name>
<files>
  src/screens/home/HomeScreen.tsx
  src/components/home/PlantOfTheDay.tsx
</files>
<read_first>
  @src/screens/home/HomeScreen.tsx
  @src/components/home/PlantOfTheDay.tsx
</read_first>
<action>
1. Modify src/screens/home/HomeScreen.tsx:
   - Add import: import { StatsBar } from "@/src/components/home/StatsBar"
   - Insert <StatsBar /> between <HomeHeader /> and PlantOfTheDay section
   - Comment out <PlantOfTheDay onLearnMore={handleLearnMore} /> with note: {/* Planta del dia oculto (D-02) */}
   - Remove unused PlantOfTheDay import
   - Remove handleLearnMore function if unused

2. Modify src/components/home/PlantOfTheDay.tsx:
   - Change to: export function PlantOfTheDay(_props: PlantOfTheDayProps) { return null; }
   - Keep PlantOfTheDayProps interface for type compatibility
   - Remove unused imports
</action>
<acceptance_criteria>
- HomeScreen.tsx imports StatsBar from "@/src/components/home/StatsBar"
- HomeScreen.tsx renders <StatsBar /> before card components
- HomeScreen.tsx does NOT render <PlantOfTheDay> (commented or removed)
- PlantOfTheDay.tsx returns null
- npx tsc --noEmit exits 0
</acceptance_criteria>
<verify>
  <automated>grep -q "StatsBar" src/screens/home/HomeScreen.tsx && grep -q "<StatsBar" src/screens/home/HomeScreen.tsx && grep -q "return null" src/components/home/PlantOfTheDay.tsx && npx tsc --noEmit 2>&1 | head -5</automated>
</verify>
<done>HomeScreen shows StatsBar, PlantOfTheDay returns null. TypeScript compiles.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> Firestore | User stats data queried from Firestore |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Information Disclosure | StatsBar reads user document | accept | All stats derived from user's own data that they already own. No PII exposed beyond what user already sees. |
| T-04-02 | Tampering | logWateringActivity writes to user doc | mitigate | Client-side operation following existing codebase pattern. Writes only to stats.streakDays and lastWateredDate. No arbitrary field injection. |
| T-04-03 | Denial of Service | getPhotosTodayCount loops large userPlants | accept | userPlants is bounded by user activity. Operations are client-side map/filter — no network cost per plant. |
</threat_model>

<verification>
- npx tsc --noEmit passes with 0 errors
- All 3 tasks committed atomically
- StatsBar renders 4 stat items with data fetched from Firestore
</verification>

<success_criteria>
1. StatsBar shows account age, photos today, watering streak, and last identification with real Firestore data
2. PlantOfTheDay returns null (hidden per D-02)
3. Individual stat errors don't crash the component (shows "---" for failed stat)
4. HomeScreen still renders all other components (LastIdentified, UserProgress, DailyMissions, etc.)

<output>
After completion, create .planning/phases/04-4-plants-cards-home-and-general-rewards/04-01-SUMMARY.md
</output>
</success_criteria>
