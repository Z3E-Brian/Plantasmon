---
phase: 04-4-plants-cards-home-and-general-rewards
plan: 03
type: execute
wave: 2
depends_on: ["04-02"]
files_modified:
  - src/components/profile/Achievements.tsx
  - src/screens/userProfile/UserProfile.tsx
  - app/journal.tsx
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "Profile screen shows all 25 achievements from Firestore with locked/unlocked visual states (D-05, D-06)"
    - "Locked achievements show muted/grayed appearance"
    - "Unlocked achievements show earned state with color"
    - "Home screen recent achievement section uses live Firestore data"
  artifacts:
    - path: "src/components/profile/Achievements.tsx"
      provides: "Achievements component with locked/unlocked visual states"
      min_lines: 80
    - path: "src/screens/userProfile/UserProfile.tsx"
      provides: "Profile screen with live achievement data from Firestore"
      contains: "Achievements"
    - path: "app/journal.tsx"
      provides: "Journal screen achievement card uses live data"
      contains: "getUserAchievements"
  key_links:
    - from: "Achievements.tsx" to: "userAchievementsService" via: "import UserAchievement type"
    - from: "UserProfile.tsx" to: "useProfile hook" via: "import useProfile"

---

<objective>
Update Profile screen achievements display to show all 25 achievements from Firestore with proper locked/unlocked visual states (D-05, D-06). Wire live data from userAchievementsService into the Achievements component.

Purpose: Make achievements visible and interactive in the UI, completing the achievement system end-to-end.
Output: Updated Achievements.tsx with locked/unlocked states, wired UserProfile with live achievement data.
</objective>

<execution_context>
@/home/brian/4_anno/Moviles/Proyecto en clase/plantasmon/.opencode/get-shit-done/workflows/execute-plan.md
@/home/brian/4_anno/Moviles/Proyecto en clase/plantasmon/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/04-4-plants-cards-home-and-general-rewards/04-CONTEXT.md

@src/components/profile/Achievements.tsx
@src/screens/userProfile/UserProfile.tsx
@src/hooks/useProfile.ts
@src/services/userAchievementsService.ts
@src/styles/themedStyles.ts
@src/constants/theme.ts
@app/journal.tsx
</context>

<tasks>

<task type="auto">
<name>Task 1: Update Achievements component with locked/unlocked visual states and live data</name>
<files>
  src/components/profile/Achievements.tsx
</files>
<read_first>
  @src/components/profile/Achievements.tsx
  @src/services/userAchievementsService.ts
  @src/styles/themedStyles.ts
</read_first>
<action>
Rewrite src/components/profile/Achievements.tsx to:

1. Accept UserAchievement[] from userAchievementsService (same props interface, already uses UserAchievement type)

2. Separate achievements into locked and unlocked:
   - Unlocked achievements: full color, earned badge emoji, normal opacity
   - Locked achievements: 40% opacity, grayscale appearance, question mark icon instead of emoji, show name
   - Add a progress indicator for achievements that have progress/target

3. Update the layout:
   - Header shows: "X / Y desbloqueados" (in Spanish)
   - 3-column grid as before
   - Each badge: icon circle, name below, count badge on unlocked ones

4. Visual states per D-06:
   - Locked: backgroundColor: theme.colors.surfaceMuted, borderColor: theme.colors.border, opacity: 0.4, icon shows "?" emoji
   - Unlocked: backgroundColor: theme.colors.primarySoft, borderColor: theme.colors.primary + "30", icon shows mapped emoji at full opacity
   - Show progress bar (thin) for achievements with progress/target defined but not yet earned

5. Add a section header between categories (Collection, Streak/Care, Usage, Special) using category dividers

6. All labels in Spanish per convention:
   - Categories: "Colección", "Racha y Cuidado", "Uso", "Especiales"
   - Header: "{earned}/{total} desbloqueados"

7. Update the existing EMOJI_MAP to include all needed emojis:
   sprout: "🌱", thumb: "👍", magnify: "🔍", globe: "🌍", water: "💧", star: "⭐", crown: "👑", tree: "🌳",
   fire: "🔥", calendar: "📅", camera: "📸", trophy: "🏆", paint: "🎨", book: "📖"
</action>
<acceptance_criteria>
- Achievements.tsx shows achievements from Firestore with locked (opacity:0.4) and unlocked (full color) states
- Header shows "{earned}/{total} desbloqueados" in Spanish
- All 4 category dividers rendered: Colección, Racha y Cuidado, Uso, Especiales
- EMOJI_MAP includes all 14 emoji keys
- npx tsc --noEmit exits 0
</acceptance_criteria>
<verify>
  <automated>grep -q "desbloqueados" src/components/profile/Achievements.tsx && grep -q "Colección\|Racha\|Uso\|Especiales" src/components/profile/Achievements.tsx && npx tsc --noEmit 2>&1 | head -5</automated>
</verify>
<done>Achievements component renders live Firestore data with locked/unlocked visual states. Category dividers present.</done>
</task>

<task type="auto">
<name>Task 2: Wire Profile screen to show achievements from Firestore</name>
<files>
  src/screens/userProfile/UserProfile.tsx
</files>
<read_first>
  @src/screens/userProfile/UserProfile.tsx
  @src/hooks/useProfile.ts
</read_first>
<action>
The useProfile hook already fetches achievements from getUserAchievements via useFocusEffect. The UserProfile.tsx already has:
```typescript
const { user, plants, achievements, loading } = useProfile()
```
and
```typescript
<Achievements achievements={achievements} />
```

Verify these are in place. If not, add them:
1. Ensure useProfile returns achievements from getUserAchievements
2. Ensure Achievements component in the profile tabs section receives achievements prop
3. The achievements should show in the "badges" tab (TabKey = "badges") which already exists in the profile

If the badges tab doesn't already render <Achievements>, add it:
- In the tab content section for activeTab === "badges", render <Achievements achievements={achievements} />
- Wrap in a View with paddingHorizontal: 20, paddingTop: 12 for consistent spacing
</action>
<acceptance_criteria>
- UserProfile.tsx passes achievements prop to Achievements component
- Achievements component renders under the "badges" tab
- useProfile hook fetches achievements from userAchievementsService
- npx tsc --noEmit exits 0
</acceptance_criteria>
<verify>
  <automated>grep -q "Achievements.*achievements" src/screens/userProfile/UserProfile.tsx && grep -q "badges" src/screens/userProfile/UserProfile.tsx && grep -q "getUserAchievements" src/hooks/useProfile.ts && npx tsc --noEmit 2>&1 | head -5</automated>
</verify>
<done>Profile screen shows achievements from Firestore with locked/unlocked states.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client -> Firestore | Achievement data read from Firestore for display |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07 | Information Disclosure | Achievements display shows achievement names/data | accept | Achievement data is public gamification content visible on the user's own profile. No sensitive data. |
| T-04-08 | Tampering | Achievement visual state determined by user data | accept | User's own achievement data from their Firestore doc — read-only display. No state is mutated in this plan. |
</threat_model>

<verification>
- npx tsc --noEmit passes with 0 errors
- Profile screen shows achievements grid with locked/unlocked states
</verification>

<success_criteria>
1. Profile screen's badges tab shows all 25 achievements from Firestore
2. Locked achievements render at 40% opacity with "?" icon
3. Unlocked achievements render at full color with proper emoji
4. Header shows "{earned}/{total} desbloqueados"
5. Category dividers separate the 4 achievement groups

<output>
After completion, create .planning/phases/04-4-plants-cards-home-and-general-rewards/04-03-SUMMARY.md
</output>
</success_criteria>
