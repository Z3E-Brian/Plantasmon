---
phase: 02-plant-core
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/collection.tsx
  - src/screens/userCollection/UserCollectionScreen.tsx
  - src/services/userPlantsService.ts
autonomous: true
requirements:
  - COLL-01
  - COLL-02
  - COLL-03
  - COLL-04
  - COLL-05
must_haves:
  truths:
    - "User can view full plant collection in list/grid"
    - "User can add plants manually without identification"
    - "User can edit plant details (nickname, notes, location)"
    - "User can remove plants from collection"
    - "User can view per-plant care history"
  artifacts:
    - path: app/collection.tsx
      provides: Collection route entry
      routes_to: UserCollectionScreen
    - path: src/screens/userCollection/UserCollectionScreen.tsx
      provides: Full collection UI with CRUD operations
      connects: Firestore userPlants -> screen
  key_links:
    - from: app/collection.tsx
      to: src/screens/userCollection/UserCollectionScreen.tsx
      via: expo-router file-based routing
      pattern: .*/collection
---

<objective>
Build complete plant collection screen with full CRUD operations.

Purpose: Users can view, add, edit, and remove plants from their personal collection
Output: Full-featured collection management screen
</objective>

<execution_context>
@/home/brian/4_anno/Moviles/Proyecto en clase/plantasmon/.opencode/get-shit-done/workflows/execute-plan.md
@/home/brian/4_anno/Moviles/Proyecto en clase/plantasmon/.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md (Phase 2 success criteria)
@.planning/REQUIREMENTS.md (COLL-01 to COLL-05)

# Existing patterns
From app/identify.tsx and app/companionPlant.tsx:
- Firebase Firestore structure: users/{uid}/userPlants[]
- UserPlant interface includes: id, nickname, notes, lastWatered, lastWeeded, firstIdentifiedAt
- Uses CURRENT_USER_ID from userService
- Uses db from src/config/firebase

# UserPlant interface (from userPlantsService)
interface UserPlant {
  id: string;
  nickname: string;
  notes: string;
  lastWatered: string | null;
  lastWeeded: string | null;
  firstIdentifiedAt: string;
  lastIdentifiedAt: string;
  personality?: string;
  location?: string;
  favorite?: boolean;
}
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create collection route and screen entry</name>
  <files>app/collection.tsx</files>
  <action>
Create app/collection.tsx as expo-router route entry:

```typescript
import UserCollectionScreen from "@/src/screens/userCollection/UserCollectionScreen";

export default UserCollectionScreen;
```

This should be a simple re-export pointing to the screen component.
  </action>
  <verify>
    <automated>ls -la app/collection.tsx</automated>
  </verify>
  <done>Collection route exists at /collection</done>
</task>

<task type="auto">
  <name>Task 2: Build full collection screen with CRUD</name>
  <files>src/screens/userCollection/UserCollectionScreen.tsx</files>
  <action>
Create UserCollectionScreen.tsx with:

**Features (COLL requirements):**
1. **View collection (COLL-01):** List/grid display of all user's plants
   - Show plant thumbnail, name, nickname
   - Favorite indicator
   - Days since last watered

2. **Add manually (COLL-02):** Add plant without camera/identification
   - Select from available plants in Firestore "plants/" collection
   - Add nickname and notes
   - One-tap add to user's collection

3. **Edit details (COLL-03):** Edit nickname, notes, location, personality
   - Inline edit or modal
   - Save updates to Firestore

4. **Remove (COLL-04):** Remove plant from collection
   - Confirmation alert
   - Remove from userPlants array in Firestore

5. **Care history (COLL-05):** View per-plant care events
   - Last watered date
   - Last fertilized/weeded date
   - Days owned

**Wire existing services:**
```typescript
import { getUserPlants, addPlant, updatePlant, removePlant, recordWatering } from "@/src/services/userPlantsService";
import { db } from "@/src/config/firebase";
import { doc, getDoc, getDocs, collection } from "firebase/firestore";
```

**UI patterns to follow:**
- Use existing ScreenWrapper from components
- Use themedStyles hook
- Follow the card/touchable patterns from companionPlant.tsx
- Use similar care row display for history
  </action>
  <verify>
    <automated>ls -la src/screens/userCollection/UserCollectionScreen.tsx</automated>
  </verify>
  <done>Collection screen with all CRUD operations working</done>
</task>

<task type="auto">
  <name>Task 3: Wire navigation to collection</name>
  <files>app/index.tsx</files>
  <action>
Ensure the home screen (app/index.tsx) has navigation to the collection screen.

- Add "Mi colección" or similar button/tap area on home screen
- Link to /collection route
- If already exists (check first), skip this task
  </action>
  <verify>
<automated>grep -l "collection\|colección" app/index.tsx || echo "NOT_FOUND"</automated>
</verify>
<done>Home screen links to collection</done>
</task>

</tasks>

<verification>
- [ ] Collection screen opens from home
- [ ] User can view all plants in collection
- [ ] User can add plant manually
- [ ] User can edit plant details
- [ ] User can remove plant from collection
- [ ] User can see care history per plant
</verification>

<success_criteria>
User can manage full plant collection: view, add, edit, remove, view care history
</success_criteria>

<output>
After completion, create `.planning/phases/02-plant-core/02-02-SUMMARY.md`
</output>