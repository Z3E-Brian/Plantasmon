---
phase: 01-authentication-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/screens/auth/loginScreen.tsx
  - src/hooks/useAuth.ts
autonomous: true
requirements:
  - AUTH-03

must_haves:
  truths:
    - "User can tap Google sign-in button on login screen"
    - "Google OAuth flow completes without error"
    - "User is authenticated via Google and redirected to home"
  artifacts:
    - path: "src/screens/auth/loginScreen.tsx"
      provides: "Working Google OAuth handleGoogleToken function"
      contains: "GoogleAuthProvider.credential"
    - path: "src/hooks/useAuth.ts"
      provides: "Cleaned up, no duplicate Google logic"
      does_not_contain: "expo-auth-session"
  key_links:
    - from: "src/screens/auth/loginScreen.tsx"
      to: "firebase/auth (GoogleAuthProvider, signInWithCredential)"
      via: "import from firebase/auth"
    - from: "src/screens/auth/loginScreen.tsx"
      to: "src/config/firebase.js (auth)"
      via: "import { auth } from @/src/config/firebase"
---

<objective>
**Fix Google OAuth login by wiring Firebase credential exchange, and clean up duplicate auth logic.**

Purpose: The Google sign-in button exists but throws "no implementado" error (D-11). The expo-auth-session flow already gets the id_token from Google, but `handleGoogleToken` doesn't exchange it for Firebase credentials. Additionally, the `useAuth.ts` hook duplicates Google logic that should be consolidated into the login screen's implementation.

Output:
- `src/screens/auth/loginScreen.tsx` — working handleGoogleToken
- `src/hooks/useAuth.ts` — cleaned up (duplicate logic removed or refactored)
</objective>

<execution_context>
@/home/brian/4_anno/Moviles/Proyecto en clase/plantasmon/.opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/01-authentication-foundation/01-CONTEXT.md

@src/screens/auth/loginScreen.tsx
@src/hooks/useAuth.ts
@src/config/firebase.js

Interfaces -- extracted from source:
- loginScreen.tsx imports Google from expo-auth-session/providers/google, uses `Google.useIdTokenAuthRequest` to get id_token via `promptAsync`. handleGoogleToken(idToken) currently throws "no implementado". The login form and Google button UI are fully implemented.
- useAuth.ts uses `Google.useIdTokenAuthRequest` independently, calls `promptAsync()` in `authGoogle()`, has useEffect logging response. Returns `{authGoogle}`.
  Decision D-12: Consolidate or remove duplicate Google logic in useAuth.ts in favor of loginScreen's inline implementation.
- firebase.js exports `auth` (initializeAuth with AsyncStorage persistence) -- already correct.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix handleGoogleToken in loginScreen.tsx to exchange id_token for Firebase credential</name>
  <files>src/screens/auth/loginScreen.tsx</files>
  <read_first>
    @src/screens/auth/loginScreen.tsx
    @src/config/firebase.js
  </read_first>
  <action>
    Per D-09 (keep expo-auth-session), D-10 (wire GoogleAuthProvider.credential + signInWithCredential), D-11 (remove throw):

    1. **Add imports** at top of file (after the existing `import * as Google from "expo-auth-session/providers/google"` line):
       ```typescript
       import { GoogleAuthProvider, signInWithCredential } from "firebase/auth";
       import { auth } from "@/src/config/firebase";
       ```

    2. **Replace the entire `handleGoogleToken` function** (lines 54-66):
       ```typescript
       const handleGoogleToken = async (idToken: string) => {
         setGoogleLoading(true);
         setError(null);
         try {
           const credential = GoogleAuthProvider.credential(idToken);
           await signInWithCredential(auth, credential);
           // el observer del _layout redirige automaticamente
         } catch (err: any) {
           setError(err.message ?? "Error al iniciar sesion con Google.");
         } finally {
           setGoogleLoading(false);
         }
       };
       ```

    3. Remove the `// TODO: Implement loginWithGoogle or handle Google login here` comment and the `throw new Error("loginWithGoogle no esta implementado.")` line.

    4. The Google button UI and `handleGooglePress` remain unchanged -- they already work by calling `promptAsync()` which triggers the OAuth flow and receives the id_token.
  </action>
  <acceptance_criteria>
    - `loginScreen.tsx` contains `import { GoogleAuthProvider, signInWithCredential } from "firebase/auth";`
    - `loginScreen.tsx` contains `import { auth } from "@/src/config/firebase";`
    - `loginScreen.tsx` contains `GoogleAuthProvider.credential(idToken)` inside handleGoogleToken
    - `loginScreen.tsx` does NOT contain `throw new Error("loginWithGoogle no está implementado")` or similar
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <done>Google OAuth flow is wired end-to-end: promptAsync -> id_token -> GoogleAuthProvider.credential -> signInWithCredential</done>
</task>

<task type="auto">
  <name>Task 2: Clean up duplicate Google auth logic in useAuth.ts</name>
  <files>src/hooks/useAuth.ts</files>
  <read_first>
    @src/hooks/useAuth.ts
  </read_first>
  <action>
    Per D-12 (consolidate or remove duplicate Google logic):

    The `useAuth.ts` hook duplicates the same `Google.useIdTokenAuthRequest` setup that `loginScreen.tsx` already handles. Since the login screen has its own inline implementation (the `promptAsync` from `useIdTokenAuthRequest`), the standalone hook is redundant.

    **Options (use judgment per Agent's Discretion):**
    - Option A (recommended): Remove the entire file content and replace with a re-export that guides consumers to use loginScreen's implementation:
      ```typescript
      // useAuth.ts -- Google auth consolidated into loginScreen.tsx
      // This file is intentionally empty. Google auth is handled inline
      // in loginScreen.tsx via expo-auth-session/providers/google.
      export {};
      ```
    - Option B: Refactor to a shared utility that both loginScreen and any future consumer import from.

    Use Option A unless there's evidence that other screens consume `useAuth` hooks. Check with `grep -r "useAuth" src/` to verify usage.

    If Option A: Keep the file as a valid TypeScript module with `export {}`.
    If Option B: Remove the `Google.useIdTokenAuthRequest` call and export a simple hook that wraps the loginScreen's pattern.

    After cleanup, the Google OAuth logic lives ONLY in loginScreen.tsx (single source of truth).
  </action>
  <acceptance_criteria>
    - `useAuth.ts` does not duplicate `Google.useIdTokenAuthRequest` from expo-auth-session
    - `useAuth.ts` is either a valid no-op module (export {}) or a lightweight re-export
    - No other files break (verify with `npx tsc --noEmit`)
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <done>Duplicate Google auth logic removed; loginScreen.tsx is the single source of truth for Google OAuth</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client to Google OAuth | User authenticates with Google, id_token is exchanged for Firebase credential |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03 | Spoofing | Google OAuth credential exchange | mitigate | Firebase Auth validates the id_token server-side via signInWithCredential; no custom validation needed |
| T-01-04 | Tampering | id_token handling | accept | id_token is passed directly from expo-auth-session response to Firebase SDK -- no manual parsing or modification |
</threat_model>

<verification>
1. `npx tsc --noEmit` passes
2. Google sign-in button triggers OAuth flow
3. handleGoogleToken no longer throws
4. No duplicate Google auth logic exists across files
</verification>

<success_criteria>
- handleGoogleToken exchanges id_token for Firebase credential
- Google OAuth flow completes without the "no implementado" error
- Duplicate Google auth logic in useAuth.ts is removed or consolidated
- TypeScript compiles clean
</success_criteria>
