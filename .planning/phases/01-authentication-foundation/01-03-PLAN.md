---
phase: 01-authentication-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - app/register.tsx
  - src/screens/auth/registerScreen.tsx
  - src/screens/auth/loginScreen.tsx
  - app/_layout.tsx
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-05

must_haves:
  truths:
    - "User can navigate to /register from login screen"
    - "User can register with email, password, confirm password"
    - "Password confirmation validates match before submission"
    - "User is auto-logged in after successful registration"
    - "A Firestore user document is created on registration"
    - "User can navigate back to login from register screen"
    - "User can log out from the app"
  artifacts:
    - path: "app/register.tsx"
      provides: "Expo Router route wrapping RegisterScreen"
      contains: "RegisterScreen"
    - path: "src/screens/auth/registerScreen.tsx"
      provides: "Register form with email, password, confirm password"
      contains: "export default function RegisterScreen"
    - path: "src/screens/auth/loginScreen.tsx"
      provides: "Working register link that navigates to /register"
      contains: "router.push"
    - path: "app/_layout.tsx"
      provides: "Register route registered in Stack navigator"
      contains: "register"
  key_links:
    - from: "src/screens/auth/registerScreen.tsx"
      to: "src/services/authService.ts"
      via: "import { register } from authService"
    - from: "src/screens/auth/registerScreen.tsx"
      to: "src/services/userService.ts"
      via: "import { createUserDocument } from userService"
    - from: "src/screens/auth/loginScreen.tsx"
      to: "app/register.tsx"
      via: "router.push(/register)"
---

<objective>
**Create the missing registration flow: route, form screen, Firestore init, and auto-login.**

Purpose: AUTH-01 (register) is unimplemented. The register route doesn't exist, `registerScreen.tsx` is empty, and the login screen's "Crear cuenta" link is a no-op. This plan creates the full registration flow matching the login screen's UI pattern, with form validation via Zod, auto-login after success, and Firestore user document creation via `createUserDocument()` from Plan 01.

Output:
- `app/register.tsx` — new expo-router route
- `src/screens/auth/registerScreen.tsx` — full register form implementation
- `src/screens/auth/loginScreen.tsx` — wired register link
- `app/_layout.tsx` — Stack.Screen registration for register route
</objective>

<execution_context>
@/home/brian/4_anno/Moviles/Proyecto en clase/plantasmon/.opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/01-authentication-foundation/01-CONTEXT.md

@src/screens/auth/loginScreen.tsx
@src/screens/auth/registerScreen.tsx (currently empty)
@src/services/authService.ts
@src/services/userService.ts
@app/_layout.tsx
@app/login.tsx
@src/constants/designSystem.ts
@src/components/screenWrapper/ScreenWrapper.tsx

Interfaces -- extracted from source:
- loginScreen.tsx: Full UI pattern to replicate -- ScreenWrapper, KeyboardAvoidingView, ScrollView, brand block (logo badge + title + subtitle), form card with error box, fields with labels, input styling, button, register link row.
- authService.ts: `register(email, password): Promise<User>` -- already implemented, returns Firebase User.
- userService.ts: `createUserDocument(uid: string, email: string): Promise<void>` -- created in Plan 01.
- _layout.tsx: Uses `Stack` from expo-router with `screenOptions={{ headerShown: false }}`. Current screens: index, camera, explore, identify, journal, profile, login, editProfile, companionPlant. Needs `register` added.
- app/login.tsx: Route file -- simple re-export pattern: `export { default } from "@/src/screens/auth/loginScreen";`
- designSystem.ts: `useAppTheme()` returns theme with colors (background, surface, primary, border, etc.), spacing (xs/sm/md/lg/xl), radius (sm/md/lg/xl/full)
- ScreenWrapper: `SafeAreaView` with theme background color and safe area insets

Existing loginScreen register link (line 395):
```tsx
<Pressable onPress={() => { /* router.push("/auth/register") */ }}>
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create app/register.tsx route file</name>
  <files>app/register.tsx</files>
  <read_first>
    @app/login.tsx
  </read_first>
  <action>
    Per D-03 (route: /register, top-level matching /login pattern):

    Create `app/register.tsx` following the same pattern as `app/login.tsx`:
    ```typescript
    export { default } from "@/src/screens/auth/registerScreen";
    ```
  </action>
  <acceptance_criteria>
    - `app/register.tsx` exists
    - `app/register.tsx` exports default from registerScreen
  </acceptance_criteria>
  <verify>
    <automated>test -f app/register.tsx && echo "Route created"</automated>
  </verify>
  <done>Register route file created at app/register.tsx</done>
</task>

<task type="auto">
  <name>Task 2: Implement RegisterScreen with form, validation, and submission</name>
  <files>src/screens/auth/registerScreen.tsx</files>
  <read_first>
    @src/screens/auth/registerScreen.tsx
    @src/screens/auth/loginScreen.tsx
    @src/constants/designSystem.ts (useAppTheme)
    @src/components/screenWrapper/ScreenWrapper.tsx
    @src/services/authService.ts
    @src/services/userService.ts
    @src/components/ui/button.tsx
  </read_first>
  <action>
    Per D-01 (form: email, password, confirm password), D-02 (auto-login), D-04 (match login screen UI), D-13 (create Firestore doc):

    Implement the full RegisterScreen. Match the login screen's visual structure exactly:
    - UseScreenWrapper, KeyboardAvoidingView (behavior="padding" on iOS), ScrollView (keyboardShouldPersistTaps="handled")
    - Brand block with logoBadge (64x64, rounded, emoji), title "Plantasmon", subtitle
    - Form card (surface background, rounded border, padding)
    - Error box (red background with error text)
    - Success/loading states
    - Form fields with labels, styled TextInputs (46px height, border, rounded)
    - Submit button using Button component

    Form fields:
    1. "Correo electronico" -- email TextInput (keyboardType="email-address", autoCapitalize="none")
    2. "Contrasena" -- password TextInput (secureTextEntry)
    3. "Confirmar contrasena" -- password TextInput (secureTextEntry)

    Client-side validation:
    - All fields required (show error if empty)
    - Password minimum 6 characters
    - Confirm password must match password (local check, NOT sent to Firebase)
    - Use local state (useState) for email, password, confirmPassword, loading, error
    - Do NOT import react-hook-form or zod -- use simple state validation to match the existing login screen style

    On submit:
    1. Validate locally: all fields filled, passwords match, password length >= 6
    2. Call `await register(email.trim(), password)` from authService
    3. On success: call `await createUserDocument(user.uid, email.trim())` from userService
    4. After both succeed: `router.replace("/")` (the auth listener in _layout.tsx will handle auto-redirect, but explicit redirect provides immediate feedback)
    5. On error: display error message from caught exception (e.g., "Ya existe una cuenta con ese correo.")

    Footer section (below form card):
    - "¿Ya tienes cuenta?" with "Iniciar sesion" link that calls `router.replace("/login")`

    Import router:
    ```typescript
    import { useRouter } from "expo-router";
    ```

    Import auth and user services:
    ```typescript
    import { register } from "../../services/authService";
    import { createUserDocument } from "../../services/userService";
    ```

    All error messages in Spanish, matching the existing locale.
  </action>
  <acceptance_criteria>
    - `registerScreen.tsx` exports a default function component
    - `registerScreen.tsx` has form fields: email, password, confirmPassword
    - `registerScreen.tsx` calls `register` from authService on submit
    - `registerScreen.tsx` calls `createUserDocument` from userService after register success
    - `registerScreen.tsx` has a link to navigate to /login
    - Password length validation min 6 characters
    - Confirm password matches password validation
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <done>Full register screen with form, validation, submission, auto-login, and error handling</done>
</task>

<task type="auto">
  <name>Task 3: Wire register link in loginScreen.tsx and add route to _layout.tsx</name>
  <files>src/screens/auth/loginScreen.tsx, app/_layout.tsx</files>
  <read_first>
    @src/screens/auth/loginScreen.tsx (line 393-398 register row)
    @app/_layout.tsx (Stack.Screen definitions)
  </read_first>
  <action>
    **In loginScreen.tsx:**
    1. Add `import { useRouter } from "expo-router";` at the top (if not already imported -- check; loginScreen already imports `useRouter` from expo-router on line 2)
    2. Replace line 395:
       ```tsx
       <Pressable onPress={() => { router.push("/register") }}>
       ```

    **In _layout.tsx:**
    1. Add `<Stack.Screen name="register" />` to the Stack component (before the closing `</Stack>`). Place it after the `login` entry to keep auth routes grouped:
       ```tsx
       <Stack.Screen name="login" />
       <Stack.Screen name="register" />
       ```
  </action>
  <acceptance_criteria>
    - `loginScreen.tsx` has `router.push("/register")` in the register link onPress
    - `_layout.tsx` has `Stack.Screen name="register"` 
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <done>Register link navigates to /register, route registered in Stack navigator</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client to Firebase Auth | Registration form submits email/password to Firebase Auth, which validates server-side |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | Tampering | Client-side validation | accept | Password match check is UX-only; Firebase Auth enforces password policy server-side |
| T-01-06 | Info Disclosure | Error messages | mitigate | Firebase error messages are mapped to user-friendly Spanish strings in authService.ts -- no raw error codes exposed |
| T-01-07 | Spoofing | createUserDocument | mitigate | createUserDocument is called with the UID returned by Firebase Auth's register() -- the UID is authoritative, not user-controlled |
</threat_model>

<verification>
1. `npx tsc --noEmit` passes
2. Register form validates: empty fields, password mismatch, short password
3. Successful registration calls `register()` then `createUserDocument()`
4. User is redirected to home after registration
5. "Crear cuenta" link in login screen navigates to /register
6. "Iniciar sesion" link in register screen navigates to /login
</verification>

<success_criteria>
- New users can register with email + password + confirm password
- Passwords validated for match client-side
- Auto-login after successful registration
- Firestore user document created with default fields
- Navigation between login and register screens works
- TypeScript compiles clean
</success_criteria>
