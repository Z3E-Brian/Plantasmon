---
phase: 01-authentication-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/services/userService.ts
  - src/services/userPlantsService.ts
  - src/services/userAchievementsService.ts
autonomous: true
requirements:
  - HOME-01
  - HOME-02
  - HOME-03
  - HOME-04
  - HOME-05
  - PROF-01
  - PROF-03
  - PROF-04
  - AUTH-04

must_haves:
  truths:
    - "User sees their own data on Home dashboard (no more shared u_001 data)"
    - "User's own plants appear in collection on Profile"
    - "User's achievements are shown for the logged-in user"
    - "Newly registered users get a Firestore document created on signup"
    - "Auth persistence works across app restarts"
  artifacts:
    - path: "src/services/userService.ts"
      provides: "getCurrentUserId() helper + createUserDocument() + dynamic UID in all functions"
      contains: "export function getCurrentUserId"
    - path: "src/services/userPlantsService.ts"
      provides: "Dynamic user UID instead of hardcoded u_001"
      does_not_contain: "CURRENT_USER_ID"
    - path: "src/services/userAchievementsService.ts"
      provides: "Dynamic user UID instead of hardcoded u_001"
      does_not_contain: "CURRENT_USER_ID"
  key_links:
    - from: "src/services/userService.ts"
      to: "src/config/firebase.js (auth)"
      via: "import { auth } from @/src/config/firebase"
    - from: "src/services/userPlantsService.ts"
      to: "src/services/userService.ts"
      via: "import { getCurrentUserId } from ./userService"
---

<objective>
**Replace hardcoded `CURRENT_USER_ID = "u_001"` with dynamic Firebase Auth UID across all services, and add new-user initialization.**

Purpose: Currently all users see the same data (`u_001`), making personalization impossible. This plan creates a `getCurrentUserId()` helper that reads `auth.currentUser?.uid`, migrates all three service files to use it, removes the `u_001` constant, and adds `createUserDocument()` for new users.

Output:
- `src/services/userService.ts` — updated with helper + new user doc creation
- `src/services/userPlantsService.ts` — updated to use dynamic UID
- `src/services/userAchievementsService.ts` — updated to use dynamic UID
</objective>

<execution_context>
@/home/brian/4_anno/Moviles/Proyecto en clase/plantasmon/.opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-authentication-foundation/01-CONTEXT.md

@src/services/userService.ts
@src/services/userPlantsService.ts
@src/services/userAchievementsService.ts
@src/config/firebase.js

Interfaces -- extracted from source:
- userService.ts: exports `CURRENT_USER_ID = "u_001"`, `getUserProfile(userId = CURRENT_USER_ID)`, `updateUserSettings(userId = CURRENT_USER_ID, ...)`, `updateUserBio(userId = CURRENT_USER_ID, ...)`, `getAccountAge(Date)`, `logWateringActivity(userId = CURRENT_USER_ID)`
- userPlantsService.ts: imports `CURRENT_USER_ID`, exports `getUserPlants`, `updateUserPlant`, `setCompanionPlant`, `togglePlantFavorite`, `addUserPlant` (uses CURRENT_USER_ID inside), `getPhotosTodayCount`, `getLastIdentification`
- userAchievementsService.ts: imports `CURRENT_USER_ID`, exports `getUserAchievements`, `seedAchievements`
- firebase.js: exports `auth` (initializeAuth with AsyncStorage persistence) and `db` (getFirestore)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create getCurrentUserId() helper and createUserDocument() in userService.ts</name>
  <files>src/services/userService.ts</files>
  <read_first>
    @src/services/userService.ts
    @src/config/firebase.js
  </read_first>
  <action>
    Per D-05 (getCurrentUserId helper), D-07 (remove CURRENT_USER_ID), D-08 (keep explicit userId optional):

    1. Remove line 9: `export const CURRENT_USER_ID = "u_001";`

    2. Add auth import. Change line 1 from:
       `import { db } from "@/src/config/firebase";`
       to:
       `import { auth, db } from "@/src/config/firebase";`

       Add `setDoc` and `serverTimestamp` to Firestore imports. Replace:
       `import { Timestamp, doc, getDoc, updateDoc } from "firebase/firestore";`
       with:
       `import { Timestamp, doc, getDoc, updateDoc, setDoc, serverTimestamp } from "firebase/firestore";`

    3. Add before UserProfile interface (after imports):
       ```
       export function getCurrentUserId(): string {
         const uid = auth.currentUser?.uid;
         if (!uid) throw new Error("Usuario no autenticado");
         return uid;
       }
       ```

    4. For ALL functions with `userId: string = CURRENT_USER_ID`:
       - Change signature to `userId?: string` (remove default)
       - Add `const resolvedUserId = userId ?? getCurrentUserId();` at top of try block
       - Replace all references to the `userId` parameter inside the function body with `resolvedUserId`
       - Functions to update: `getUserProfile`, `updateUserSettings`, `updateUserBio`, `logWateringActivity`
       - Do NOT change `getAccountAge()` (takes Date, not userId)

    5. Add createUserDocument() at end of file (per D-13):
       ```
       export async function createUserDocument(uid: string, email: string): Promise<void> {
         try {
           const ref = doc(db, "users", uid);
           const snapshot = await getDoc(ref);
           if (snapshot.exists()) { console.log("User doc exists:", uid); return; }
           const displayName = email.split("@")[0] || "";
           await setDoc(ref, {
             displayName, email, username: "",
             aboutme: "Amante de las plantas", location: "Sin ubicacion",
             avatarUrl: "", rarestFind: "Por descubrir", careScore: 0,
             createdAt: serverTimestamp(),
             stats: { level: 1, xp: 0, xpToNextLevel: 1000, plantsIdentified: 0, streakDays: 0, lastWateredDate: null },
             settings: { themeId: "theme_forest", titleId: "", frameId: "" },
             userPlants: [], userAchievements: [],
           });
         } catch (error) { console.error("Error creating user doc:", error); throw error; }
       }
       ```

    6. Keep all existing logic identical. Do not refactor anything beyond the auth resolution change.
  </action>
  <acceptance_criteria>
    - `src/services/userService.ts` contains `export function getCurrentUserId`
    - `src/services/userService.ts` contains `export async function createUserDocument`
    - `src/services/userService.ts` contains `const resolvedUserId = userId ?? getCurrentUserId()`
    - `src/services/userService.ts` contains `serverTimestamp`
    - `src/services/userService.ts` no longer contains `CURRENT_USER_ID` (outside comments)
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <done>userService.ts has `getCurrentUserId()`, `createUserDocument()`, no `CURRENT_USER_ID`, all functions use `resolvedUserId` pattern</done>
</task>

<task type="auto">
  <name>Task 2: Migrate userPlantsService.ts and userAchievementsService.ts to dynamic UID</name>
  <files>src/services/userPlantsService.ts, src/services/userAchievementsService.ts</files>
  <read_first>
    @src/services/userPlantsService.ts
    @src/services/userAchievementsService.ts
  </read_first>
  <action>
    Per D-06 (services use helper internally), D-07 (remove CURRENT_USER_ID):

    For userPlantsService.ts:
    1. Replace line 9: `import { CURRENT_USER_ID } from "./userService";` → `import { getCurrentUserId } from "./userService";`
    2. For EACH function with `userId: string = CURRENT_USER_ID`:
       - Change signature: `userId?: string` (remove default)
       - Add `const resolvedUserId = userId ?? getCurrentUserId();` at top of try block
       - Replace `userId` parameter references with `resolvedUserId` in function body
       - Functions: `getUserPlants`, `updateUserPlant`, `setCompanionPlant`, `togglePlantFavorite`, `getPhotosTodayCount`, `getLastIdentification`
    3. For `addUserPlant()`: Change the destructured default `userId = CURRENT_USER_ID` to `userId?: string` in `AddPlantFromIdInput`, then add `const resolvedUserId = userId ?? getCurrentUserId();` at top of try block and use `resolvedUserId`.

    For userAchievementsService.ts:
    1. Replace line 9: `import { CURRENT_USER_ID } from "./userService";` → `import { getCurrentUserId } from "./userService";`
    2. `getUserAchievements(userId?: string)`: change signature, add `const resolvedUserId = userId ?? getCurrentUserId();`
    3. Leave `seedAchievements()` unchanged (no userId needed).
  </action>
  <acceptance_criteria>
    - Both files import `getCurrentUserId` instead of `CURRENT_USER_ID`
    - Both files use `resolvedUserId` pattern
    - Neither file contains `CURRENT_USER_ID`
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit --pretty 2>&1 | head -20</automated>
  </verify>
  <done>Both service files use dynamic UID, TypeScript compiles without errors</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| client to Firestore | Firestore reads use the authenticated user's UID via getCurrentUserId() |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Spoofing | getCurrentUserId() | accept | Throws if auth.currentUser is null; Firestore security rules are the real auth boundary |
| T-01-02 | Info Disclosure | User doc query | mitigate | getCurrentUserId() guarantees caller uses authenticated user's UID, preventing accidental cross-user access |
</threat_model>

<verification>
1. `npx tsc --noEmit` passes
2. No `CURRENT_USER_ID` remains in any service file
3. All exported functions resolve userId from auth when not explicitly passed
4. `createUserDocument()` creates Firestore doc with fields matching existing component expectations
</verification>

<success_criteria>
- Hardcoded `u_001` removed from all services
- Every service resolves userId from Firebase Auth
- `createUserDocument()` creates user doc with expected fields
- TypeScript compiles clean
</success_criteria>
